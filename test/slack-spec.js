'use strict';
var slack = require('../lib/slack');

//jshint expr:true
const chai = require('chai');
// chai.should();

describe.only('name or description of test', () => {
    //beforeEach ( () =>{});
    //afterEach ( () =>{});
    it('should ...', () => {
        resp = slack.helloSlack();
    });
});